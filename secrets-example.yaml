# ESPHome secrets example file
# IMPORTANT: Copy this file to secrets.yaml and replace with your actual values
# DO NOT commit your actual secrets.yaml file to version control!

name: "kc868-a6"

# WiFi credentials
wifi_ssid: "wifi_ssid"
wifi_password: "wifi_password"

fallback_ssid: "fallback_ssid"
fallback_password: "fallback_password"

# API encryption key 
# generate in browser: https://esphome.io/components/api.html
# Windows: powershell -Command "[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [Byte[]])"
# Linux/Mac: head -c 32 /dev/random | base64
api_encryption_key: "jTD+Q+Pjna4QBz06SJKqfLe4wuc8fdqIu9Dn8XF/150="

# OTA password
ota_password: "YourOTAPassword"

# Web server credentials
web_username: "admin"
web_password: "admin"

# MQTT credentials (if used)
mqtt_broker: "192.168.0.11"
mqtt_username: "mqtt_username"
mqtt_password: "mqtt_password"
